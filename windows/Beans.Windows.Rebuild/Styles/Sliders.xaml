<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:primitives="using:Microsoft.UI.Xaml.Controls.Primitives">

    <Style x:Key="MusicSliderThumbStyle" TargetType="primitives:Thumb">
        <Setter Property="Width" Value="12" />
        <Setter Property="Height" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:Thumb">
                    <Ellipse Fill="{StaticResource Primary600Brush}" Stroke="White" StrokeThickness="2" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MusicSliderStyle" TargetType="Slider">
        <Setter Property="Height" Value="28" />
        <Setter Property="MinWidth" Value="80" />
        <Setter Property="IsTabStop" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid x:Name="Root" Background="Transparent">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="HorizontalThumb.Width" Value="15" />
                                        <Setter Target="HorizontalThumb.Height" Value="15" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="HorizontalThumb.Width" Value="16" />
                                        <Setter Target="HorizontalThumb.Height" Value="16" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters><Setter Target="Root.Opacity" Value="0.4" /></VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="FocusStates">
                                <VisualState x:Name="Unfocused" />
                                <VisualState x:Name="Focused">
                                    <VisualState.Setters>
                                        <Setter Target="FocusBorder.BorderBrush" Value="{StaticResource FocusRingBrush}" />
                                        <Setter Target="FocusBorder.Opacity" Value="1" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="PointerFocused" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid x:Name="HorizontalTemplate" Margin="4,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Rectangle x:Name="HorizontalTrackRect" Grid.ColumnSpan="3" Height="4" VerticalAlignment="Center" Fill="{StaticResource TrackInactiveBrush}" RadiusX="2" RadiusY="2" />
                            <Rectangle x:Name="HorizontalDecreaseRect" Height="4" VerticalAlignment="Center" Fill="{StaticResource Primary600Brush}" RadiusX="2" RadiusY="2" />
                            <primitives:Thumb x:Name="HorizontalThumb" Grid.Column="1" Style="{StaticResource MusicSliderThumbStyle}" />
                        </Grid>
                        <Border x:Name="FocusBorder" Margin="0,2" BorderBrush="Transparent" BorderThickness="1" CornerRadius="4" Opacity="0" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="VolumeSliderStyle" TargetType="Slider" BasedOn="{StaticResource MusicSliderStyle}">
        <Setter Property="MinWidth" Value="92" />
    </Style>
</ResourceDictionary>
