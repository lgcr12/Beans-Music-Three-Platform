<UserControl
    x:Class="Beans.Windows.Rebuild.Pages.Album.AlbumPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Beans.Windows.Rebuild.Pages.Artist"
    xmlns:badge="using:Beans.Windows.Rebuild.Controls.PlatformBadge">
    <Grid Background="{StaticResource ContentBackgroundBrush}"><ScrollViewer HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"><StackPanel Margin="24,24,24,32" Spacing="22">
        <Grid ColumnSpacing="22"><Grid.ColumnDefinitions><ColumnDefinition Width="210" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><Border Width="210" Height="210" CornerRadius="8"><Image Source="{x:Bind CoverUri}" Stretch="UniformToFill" /></Border><StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="9"><TextBlock Text="专辑" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="{x:Bind Title}" Style="{StaticResource PageTitleTextStyle}" TextWrapping="Wrap" /><TextBlock Text="{x:Bind Artist}" Style="{StaticResource SecondaryTextStyle}" /><badge:PlatformBadge Platform="{x:Bind Platform}" HorizontalAlignment="Left" /><TextBlock Text="公开专辑详情 · 播放仍需对应平台授权" Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap" /><TextBlock Text="{x:Bind StatusText, Mode=OneWay}" Style="{StaticResource CaptionTextStyle}" /></StackPanel></Grid>
        <TextBlock Text="歌曲" Style="{StaticResource SectionTitleTextStyle}" /><Border Style="{StaticResource SurfacePanelStyle}" Padding="8"><ListView ItemsSource="{x:Bind Tracks}" IsItemClickEnabled="True" ItemClick="Track_ItemClick" SelectionMode="None"><ListView.ItemTemplate><DataTemplate x:DataType="models:DetailTrackPreview"><Grid MinHeight="62" Padding="10,5" ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="170" /><ColumnDefinition Width="80" /><ColumnDefinition Width="150" /></Grid.ColumnDefinitions><StackPanel VerticalAlignment="Center"><TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" /><TextBlock Text="{x:Bind Album}" Style="{StaticResource CaptionTextStyle}" /></StackPanel><TextBlock Grid.Column="1" Text="{x:Bind Artist}" VerticalAlignment="Center" /><TextBlock Grid.Column="2" Text="{x:Bind Duration}" VerticalAlignment="Center" Style="{StaticResource CaptionTextStyle}" /><TextBlock Grid.Column="3" Text="{x:Bind RestrictionState}" VerticalAlignment="Center" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" /></Grid></DataTemplate></ListView.ItemTemplate></ListView></Border>
    </StackPanel></ScrollViewer></Grid>
</UserControl>
