<UserControl
    x:Class="Beans.Windows.Rebuild.Pages.Favorites.FavoritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:library="using:Beans.Windows.Rebuild.Services.Library">
    <Grid Background="{StaticResource ContentBackgroundBrush}">
        <ScrollViewer HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24,24,24,32" Spacing="18">
                <Grid>
                    <StackPanel Spacing="5">
                        <TextBlock Text="收藏夹" Style="{StaticResource PageTitleTextStyle}" />
                        <TextBlock Text="保存在本机的真实收藏，不保存播放地址或平台凭证" Style="{StaticResource SecondaryTextStyle}" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Spacing="3">
                        <TextBlock x:Name="CountText" Text="0 项" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Right" />
                        <TextBlock x:Name="StatusText" Text="正在读取本地收藏…" Style="{StaticResource CaptionTextStyle}" HorizontalAlignment="Right" />
                    </StackPanel>
                </Grid>

                <Border x:Name="EmptyPanel" Style="{StaticResource SurfacePanelStyle}" Padding="32">
                    <StackPanel HorizontalAlignment="Center" Spacing="10">
                        <FontIcon Glyph="&#xEB51;" FontSize="32" Foreground="{StaticResource TextTertiaryBrush}" />
                        <TextBlock Text="还没有收藏内容" Style="{StaticResource PanelTitleTextStyle}" HorizontalAlignment="Center" />
                        <TextBlock Text="收藏成功后会安全地保存在本机，并显示在这里。" Style="{StaticResource SecondaryTextStyle}" TextWrapping="Wrap" HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>

                <Border x:Name="FavoritesPanel" Style="{StaticResource SurfacePanelStyle}" Padding="8" Visibility="Collapsed">
                    <ListView x:Name="FavoritesList" SelectionMode="None" IsItemClickEnabled="True" ItemClick="Favorite_ItemClick">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="library:FavoriteEntry">
                                <Grid MinHeight="68" Padding="8,6" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="50" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="110" />
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Width="50" Height="50" CornerRadius="6" Background="{StaticResource SurfaceSecondaryBrush}">
                                        <Image Source="{x:Bind CoverUri}" Stretch="UniformToFill" />
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="3">
                                        <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" MaxLines="1" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{x:Bind Artist}" Style="{StaticResource CaptionTextStyle}" MaxLines="1" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{x:Bind KindText}" Style="{StaticResource SecondaryTextStyle}" VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="3" VerticalAlignment="Center" Spacing="3">
                                        <TextBlock Text="{x:Bind PlatformName}" Style="{StaticResource SecondaryTextStyle}" />
                                        <TextBlock Text="{x:Bind AddedAtText}" Style="{StaticResource CaptionTextStyle}" />
                                    </StackPanel>
                                    <Button Grid.Column="4" Content="移除" Tag="{x:Bind}" Click="RemoveFavorite_Click" Style="{StaticResource GhostButtonStyle}" VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
                <TextBlock Text="QQ 音乐和网易云音乐收藏仅保留平台标识与内容标识；这不是向平台账号写入收藏。" Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
